{
  "$schema": "https://deno.land/x/denon@2.4.8/schema.json",
  "scripts": {
    "test": {
      "cmd": "deno test index.ts",
      "desc": "Test Uwuifier-server"
    },
    "start": {
      "cmd": "deno run index.ts",
      "desc": "Start Uwuifier-server"
    }
  },
  "allow": {
    "env": "MYSQL_HOSTNAME,MYSQL_USERNAME,MYSQL_PASSWORD,MYSQL_PORT,MYSQL_DATABASE,DENO_DEPLOYMENT_ID",
    "net": "188.166.88.206:25060,0.0.0.0:8080,api.ipify.org",
    "read": ".env"
  },
  "logger": {
    "quiet": true,
    "debug": true
  }
}
